#include <iostream>
#include <memory>
#include <vector>

using namespace std;
// class allocator : xmemory---> line 738

// 内存对齐
// 一般内存池中开辟的内存大小都是8的倍数, 当对象大小不是8的倍数时需要对齐

// 嵌入式指针
// 对象本身的大小必须大于 4 byte
int main()
{
    allocator<int> alloc;
    std::vector<int> v;
    v.push_back()
    if(true)
    {
        ///
    }
}